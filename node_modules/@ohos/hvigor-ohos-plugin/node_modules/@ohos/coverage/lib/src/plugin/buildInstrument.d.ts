import { sourceMap } from '../types/instrument';
/**
 * 测试框架代码覆盖率收集代码插装插装 (编译构建调用)
 * @param projectConfig
 */
export declare const buildInstrument: () => {
    name: string;
    buildStart: typeof buildStart;
    transform: typeof transform;
    shouldInvalidCache: typeof shouldInvalidCache;
};
/**
 * 功能：
 * @param {any} this
 */
declare function buildStart(this: any): Promise<void>;
interface transformReturn {
    code: string;
    map: sourceMap;
}
/**
 * 功能：
 * @param {any} this
 * @param {string} code
 * @param {string} id
 * @returns
 */
declare function transform(this: any, code: string, id: string): Promise<transformReturn | undefined>;
declare function shouldInvalidCache(): Promise<boolean>;
export {};
