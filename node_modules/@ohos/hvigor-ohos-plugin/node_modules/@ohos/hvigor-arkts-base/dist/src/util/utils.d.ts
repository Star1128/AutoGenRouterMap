/**
 * 从当前路径一级一级往上级目录寻找 package.json
 *
 * @param currentPath 当前路径
 *
 * @return package.json path or undefined
 */
export declare function findPackageJsonPath(currentPath: string): string | undefined;
/**
 * 按照优先级补充文件后缀查找文件
 * @param filePath
 * @param extensions
 */
export declare const findSuitableFilePath: (filePath: string, extensions: string[]) => string;
