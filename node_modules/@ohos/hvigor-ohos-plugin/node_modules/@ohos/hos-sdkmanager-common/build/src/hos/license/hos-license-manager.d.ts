import { AbstractLicenseManager, CommonNetClient, License, Progress, SdkSettings } from '@ohos/sdkmanager-common';
export declare class HosLicenseManager extends AbstractLicenseManager {
    private _sdkSettings;
    private _netClient;
    constructor(sdkSettings: SdkSettings, netClient: CommonNetClient);
    protected getLicensesRootLocation(): string;
    protected getReadLocalHashErrorMessage(licensesLocation: string): string;
    retrieveLicenses(licenseIds: string[], progress: Progress): Promise<Map<string, License>>;
    private _createLicenseReqDto;
}
