import { AbstractLocalComponentLoader, Component, Progress } from '@ohos/sdkmanager-common';
export declare class HosLocalComponentLoader extends AbstractLocalComponentLoader {
    private static MAX_SCAN_DEPTH;
    private _inCompatibleComponents;
    constructor(sdkRoot: string, progress?: Progress);
    protected parseSdks(potentialUniPackages: string[]): Component[];
    private _addToMap;
    protected getUniPackageName(): string;
    private _convertComponent;
    private _configMeta;
    /**
     * 判断元数据中的metaVersion是否是可兼容的版本
     * @param dto
     * @private
     */
    private _isCompatibleMetaVersion;
    /**
     * 校验sdk的路径
     *
     * @param component
     * @protected
     */
    private _isComponentInRightPlace;
    private _getExpectedLocation;
    protected getSdkRoot(): string;
    /**
     * 获取不兼容的sdk
     */
    getInCompatibleComponents(): Array<Component>;
    private _isIncompatibleComponent;
}
