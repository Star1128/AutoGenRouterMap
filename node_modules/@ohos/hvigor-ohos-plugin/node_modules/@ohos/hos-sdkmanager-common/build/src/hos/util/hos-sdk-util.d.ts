import { HosLocalComponentLoader } from '../loader/hos-local-component-loader';
/**
 * lib库检查SDK的兼容性
 * @param hosLocalLoader
 * @param errorMsg
 */
export declare function checkSdkCompatibility(hosLocalLoader: HosLocalComponentLoader, errorMsg: string): void;
export declare function decoratePath(path: string): string;
