import { CommonNetClient, Component, ComponentTask, SdkSettings } from '@ohos/sdkmanager-common';
/**
 * 创建HarmonyOS SDK的安装任务
 * @param component Component
 * @param settings SdkSettings
 * @param netClient CommonNetClient
 */
export declare function createHosInstallTask(component: Component, settings: SdkSettings, netClient: CommonNetClient): ComponentTask;
