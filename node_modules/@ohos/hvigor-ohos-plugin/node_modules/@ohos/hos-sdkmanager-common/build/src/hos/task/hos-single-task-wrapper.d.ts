import { ComponentTask, ErrorTip, SingleTaskWrapper } from '@ohos/sdkmanager-common';
export declare class HosSingleTaskWrapper extends SingleTaskWrapper {
    constructor(componentTask: ComponentTask);
    protected getDuplicateTaskErrorTip(lockFile: string): ErrorTip;
    getWorkDir(): string;
}
