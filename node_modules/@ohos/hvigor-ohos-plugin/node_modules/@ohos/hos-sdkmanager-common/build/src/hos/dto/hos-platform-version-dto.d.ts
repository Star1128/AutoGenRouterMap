import { PlatformVersion } from '../api/hos-component';
export declare class HosPlatformVersionDto implements PlatformVersion {
    version: string;
    compareTo(other: PlatformVersion): number;
    getVersion(): string;
    equals(other: PlatformVersion): boolean;
}
