interface DelOptions {
    hook?: string;
    runOnce?: boolean;
    targets?: string[];
}
export default function delPlugin(options?: DelOptions): {
    [x: string]: string | (() => Promise<void>);
    name: string;
};
export {};
