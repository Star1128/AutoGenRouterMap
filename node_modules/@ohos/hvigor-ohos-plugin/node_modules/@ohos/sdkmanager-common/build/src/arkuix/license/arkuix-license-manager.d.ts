import { AbstractLicenseManager, License } from '../../core/license/license-manager';
import { SdkSettings } from '../../core/domain/sdk-settings';
import { Progress } from '../../core/log/progress';
import { CommonNetClient } from '../../core/net/common-net-client';
export declare class ArkUIXLicenseManager extends AbstractLicenseManager {
    private _sdkSettings;
    private _netClient;
    constructor(sdkSettings: SdkSettings, netClient: CommonNetClient);
    protected getReadLocalHashErrorMessage(licensesLocation: string): string;
    protected getLicensesRootLocation(): string;
    retrieveLicenses(licenseIds: string[], progress: Progress): Promise<Map<string, License>>;
    private _createReqBody;
}
