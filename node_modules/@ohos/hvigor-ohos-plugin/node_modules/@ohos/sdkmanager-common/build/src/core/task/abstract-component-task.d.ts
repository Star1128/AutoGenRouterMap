import { ComponentTask } from './component-task';
import { Progress } from '../log/progress';
import { Component } from '../api/component';
export declare abstract class AbstractComponentTask implements ComponentTask {
    protected readonly component: Component;
    protected constructor(component: Component);
    execute(progress: Progress): Promise<void>;
    abstract getComponent(): Component;
    abstract getSdkRoot(): string;
    getRootWorkDir(): string;
    getWorkDir(): string;
    prepare(progress: Progress): Promise<void>;
    complete(progress: Progress): Promise<void>;
}
