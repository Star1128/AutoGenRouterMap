import { Progress } from './progress';
export declare class SimpleProgress implements Progress {
    debug(log: string): void;
    error(log: string): void;
    fatal(log: string): void;
    info(log: string): void;
    warn(log: string): void;
}
