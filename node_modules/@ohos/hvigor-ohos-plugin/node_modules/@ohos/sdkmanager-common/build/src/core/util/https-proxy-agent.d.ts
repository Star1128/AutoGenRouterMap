/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import https from 'https';
import { URL } from 'url';
import { Socket } from 'net';
export interface ProxyAgentOptions extends https.AgentOptions {
    proxy: string;
}
export declare class HttpsProxyAgent extends https.Agent {
    proxy: URL;
    constructor(options: ProxyAgentOptions);
    createConnection(options: any, callback: (err: Error | null, socket: Socket | null) => void): void;
}
