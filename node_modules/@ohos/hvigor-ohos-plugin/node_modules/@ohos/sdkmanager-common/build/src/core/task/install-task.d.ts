import { SdkSettings } from '../domain/sdk-settings';
import { ComponentTask } from './component-task';
import { Progress } from '../log/progress';
export interface InstallTask extends ComponentTask {
    /**
     * getSdkSettings
     */
    getSdkSettings(): SdkSettings;
    /**
     * 获取Component安装时的临时存放目录
     */
    getTempComponentPath(): string;
    /**
     * 下载文件
     * @param progress
     * @protected
     * @return 下载后文件的存放路径
     */
    download(progress: Progress): Promise<string>;
}
