import { PathAndApiVersion } from '../api/path-and-apiversion';
import { Component } from '../api/component';
export declare class PathAndApiMap extends Map<PathAndApiVersion, Component> {
    constructor(entries?: readonly (readonly [PathAndApiVersion, Component])[] | null);
    get(key: PathAndApiVersion): Component | undefined;
    has(key: PathAndApiVersion): boolean;
}
